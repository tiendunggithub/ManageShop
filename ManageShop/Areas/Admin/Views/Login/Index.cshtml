@model ManageShop.Models.User
@{
    Layout = "_Login";
}


    <div class="login-dark">
        @using (Html.BeginForm("Verify", "Login",FormMethod.Post))
        {
        <form method="post">
            <h2 class="sr-only">Login Form</h2>
            <div class="illustration"><i class="icon ion-ios-locked-outline"></i></div>
            <div class="form-group">
                <input class="form-control" type="text" name="username" placeholder="Tên đăng nhập">
                @*@Html.EditorFor(model => model.username)*@
            </div>
            <div class="form-group">
                <input class="form-control" type="password" name="password" placeholder="Mật khẩu">
                @*@Html.PasswordFor(m => m.Password, new { @class = "form-control", @placeholder = "Password" })*@
                @*@Html.PasswordFor(model => model.password)*@
                
            </div>
            <div class="form-group">
                <button class="btn btn-primary btn-block" type="submit" name="name">Đăng nhập</button>
            </div>
            @*<a href="#" class="forgot">Forgot your email or password?</a>*@
        </form>
        }
    </div>
